{
  "name": "civicconnect",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:all": "concurrently -n \"SERVER,CLIENT,AI\" -c \"blue,green,magenta\" \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:ai\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "node server/server.js",
    "dev:ai": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir ai-service",
    "build:client": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix client install && npm --prefix server install",
    "setup": "npm install && npm --prefix client install && npm --prefix server install"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
